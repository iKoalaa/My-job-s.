Пример демонстрирует, как ConcurrentHashMap используется в многопоточном контексте. Программа создает два потока записи и 5 потоков чтения, работающих над общим экземпляром ConcurrentHashMap .
Нить Writer случайным образом изменяет Map (put и remove).

Основная программа создает и запускает 2 потока записи и 5 потоков чтения одновременно для работы с общим экземпляром ConcurrentHashMap.

Эта программа работает вечно, потому что все потоки выполняют бесконечный цикл.
Потоки Reader дают вам знать о том,что map постоянно обновляется потоками Writer.
